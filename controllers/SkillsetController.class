<?php

class SkillsetController extends BaseController {

  public function index() {

  	$page = (int) $this->getParam(0, 1);

	$skillset_wf3 = Cvform::getList('SELECT * FROM skillset_wf3');

/*	$profiles = $pagination->results;
*/		$vars = array(
			'HTML 5' => $html,
			'CSS 3' => $css,
			'PHP' => $php,
			'MySQL' => $mysql,
			'Javascript' => $javascript,
			'CMS-Wordpress ...' => $cms

		);

/*		echo '<pre>';
		print_r($profiles);
		echo '</pre>';*/

    $this->render('cv-form', $vars);

  }

}
